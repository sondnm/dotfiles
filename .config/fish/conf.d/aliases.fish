alias mux "tmuxp load -y"

alias bi "bundle install"
alias be "bundle exec"

alias solmain "solana config set --url https://api.mainnet-beta.solana.com -k ~/.config/solana/mainnet.json"
alias soldev "solana config set --url https://api.devnet.solana.com -k ~/.config/solana/devnet.json"
alias sollocal "solana config set --url http://localhost:8899 -k ~/.config/solana/localnet.json"
alias solseed "solana config set --url https://api.devnet.solana.com -k ~/.config/solana/seeded-devnet.json"
